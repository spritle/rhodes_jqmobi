<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @theme_app.name %></h1>
    <a href="<%= url_for :action => :show, :id => @theme_app.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @theme_app.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @theme_app.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="theme_app[name]" class="fieldLabel">Name</label>
            <input type="text" id="theme_app[name]" name="theme_app[name]" value="<%= @theme_app.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
